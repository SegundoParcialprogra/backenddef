namespace backenddef.Models
{
    public enum UserRole
    {
        Cliente,
        Empresa
    }

    public enum OrderStatus
    {
        Nuevo,
        Enviado,
        Entregado,
        Cancelado
    }
}